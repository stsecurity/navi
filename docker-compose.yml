services:
  homehub:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: homehub
    restart: unless-stopped
    environment:
      PORT: ${PORT:-8000}
    ports:
      - "${PORT:-8000}:8000"
    volumes:
      - ./data:/app/data
